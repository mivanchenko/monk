#!/usr/bin/env perl

# #!/usr/bin/perl

use strict;
use warnings;

#push my @commands, (
#	'git checkout master',
#	'git pull --rebase',
#	'chmod -R o-rwx ../* ../.*',
#	'chmod -R g-rwx ../.git',
#	'chmod -R g-w ../* ../.htaccess',
#	"chmod 700 $0",
#);
#
#my $command = join( ' 2>&1'."\n", @commands );
#
#print "Content-Type: text/plain\n\n";
#
#my $result = qx( $command );
#print "$result\n";

print "Content-Type: text/plain\n\n";
print 'ok';
